{% block body %}

	<p>Hello from Twig!</p>

	<h2>Users:</h2>
	<ul>
		{% for user in users %}
			<li>{{user.id}}.
				{{user.name}}</li>
		{% endfor %}
	</ul>
        <div class="col my-2">
            <a href="/" class="btn btn-primary">Home</a>
        </div>
{% endblock %}



